import React from 'react'; // Import React to define the component
import './preloader.css'; // Import CSS for the preloader styling

// Functional component to render a preloader
export default function Preloader() {
  return (
    // The preloader div which will display a loading animation
    <div id='preloader'></div>
  );
}
